### 1.线程池的核心参数和原理（14 场被问）⭐⭐⭐⭐⭐

核心线程数、最大线程数、等待队列、拒绝策略——这四个参数是保留节目。常见追问：IO 密集型和 CPU 密集型分别怎么配？无界队列会导致什么问题？空闲线程怎么回收？队列满了为什么先入队而不是先扩容线程？

核心线程并没有本质区别，没有特殊区分，主要是看当前线程计数

### 2.synchronized 的用法和锁机制（7 场被问）⭐⭐⭐⭐

三种加锁方式（方法、代码块、静态方法），静态方法加 synchronized 后类名调用和对象调用有没有竞争关系，synchronized 和 ReentrantLock 的区别。

### 3.你使用过哪些阻塞队列

阻塞队列内部使用ReentrantLock控制并发

### 4.synchonize和reeantrnanlok
补充：一个是进入block，另一个时调用aqs的方法，使用locksupport的park  unpark，进入waiting状态

### 5.CAS 原理、ABA 问题、ConcurrentHashMap 为什么同时用 CAS 和 synchronized、ConcurrentHashMap 和 Hashtable 的区别。
补充：可以采用复合值得方式，高32位存放版本号，低32位存放数据，拼接成long类型

“因为当发生哈希碰撞时，操作涉及遍历链表、尾插法修改指针，甚至将链表转为红黑树等一连串非原子的操作。如果用 CAS 去硬做，代码复杂度会极高，且在高竞争下自旋开销巨大。因此，使用 synchronized 锁住桶头节点是更合理、更清晰的选择。”

“这种 CAS + synchronized 的组合，本质上是对 JDK 1.7 分段锁（Segment）设计的优化。1.8 将锁的粒度从‘段’细化到了单个桶的头节点，并发度更高。同时，JDK 1.6 后 synchronized 的性能经过锁升级优化已经非常好了，结合 CAS 的无锁化插入，让 ConcurrentHashMap 在安全和性能之间找到了一个很好的平衡点。”

### 6.jvm相关
JDK (Java Development Kit) - Java 开发工具包
│
├── JRE (Java Runtime Environment) - Java 运行环境
│   │
│   ├── JVM (Java Virtual Machine) - Java 虚拟机 ← 最底层
│   │
│   └── 核心类库 (rt.jar 等)
│
└── 开发工具 (javac, jar, javap, jconsole 等)

bootstrap类加载器是由hotspot提供的加载最重要最基础类（例如jre核心类库中的jar文件中的类）的加载器，

### 7.类的生命周期，三个生命周期分别干了什么
连接阶段里面又可以分为三个阶段，初始化阶段对堆里面的的静态变量赋值，执行静态代码块，实际上是执行字节码文件里面方法中的clinit方法

### 8.为什么要打破双亲委派机制
同名类无法加载，怎么做：继承ClassLoader类，并且重写loadclass方法，即可。在双亲委派机制下，同一个类限定名，即被认为是同一个类，即使 .class 文件内容完全不同，也不会重新加载。

使用双亲委派又有什么好处：1.防止一个类被重复加载2.安全：如果自己定义了不安全的同名类想要注入进去搞破坏如jar包中有同名的类java.lang.String，这个核心类库已经被启动类加载器加载

### 9区分几个概念
字符串常量池，运行时常量池，Class 文件常量池，java虚拟栈中的局部变量表

<img width="1569" height="732" alt="image" src="https://github.com/user-attachments/assets/f9e7086e-47cc-4664-8d9b-f6ed41bc2aa0" />
虚拟机栈中每个栈帧都由局部变量表，操作数栈，帧数据组成

<img width="1384" height="634" alt="image" src="https://github.com/user-attachments/assets/5a27d247-74ee-4b1f-a4ef-a3c76e583202" />
元信息就是字节码文件加载过来形成的instanceKlass


### 10当我们在创建一个对象时
jvm栈中的局部变量表就会创建一个变量，而他的cell中并不是像基本变量一样的存放的是具体的值，而是具体对象在堆中的地址

局部变量表中的变量都存放在一个类似数据的结构中，局部变量表中就存放这些变量的一些信息，比如在下标的位置在哪

### 11垃圾回收算法评价标准
1.吞吐率2.最大回收时间，3.堆的利用率

### 12.RR
<img width="1288" height="504" alt="image" src="https://github.com/user-attachments/assets/001462f8-e67f-44f8-adee-e5f6827edf85" />
